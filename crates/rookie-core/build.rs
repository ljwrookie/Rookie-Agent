// NAPI-RS build script (P3-T1)
// Conditionally compiles NAPI bindings when napi feature is enabled

fn main() {
    // Stub build script for future NAPI-RS integration
    // When napi-rs is added as a dependency, this will:
    // 1. Generate TypeScript definitions
    // 2. Build the .node addon
    println!("cargo:rerun-if-changed=build.rs");
}
